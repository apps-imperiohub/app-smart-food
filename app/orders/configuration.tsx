import FormDirection from "@/components/AboutMe/FormDirection";
import { View } from "react-native";

const configurationScreen = () => {
  return (
    <View>
      <FormDirection />
    </View>
  );
};

export default configurationScreen;
